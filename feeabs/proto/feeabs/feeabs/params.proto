syntax = "proto3";
package feeabs.feeabs;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "feeabs/x/feeabs/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "feeabs/x/feeabs/Params";
  option (gogoproto.equal) = true;

  
}